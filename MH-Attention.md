# Multi-Head Attention（多頭注意力）的優點（Markdown 版）

## 1) 核心優點
1. **學習多種關係，提升表達力**
   - 單頭本質上是一次加權平均（近似低秩映射）；多頭在不同子空間各自建模，最後 Concat→\(W_O\) 混合，**提高有效秩**，同時捕捉語法/語義/位置/對齊等多樣關係。
2. **子空間專精（Feature Disentangling）**
   - 每個頭擁有獨立的 \(W_Q^{(i)}, W_K^{(i)}, W_V^{(i)}\)，自然形成**分工**，避免單一大頭將所有模式糾纏在一起。
3. **更穩定的學習與梯度流**
   - 多個頭分攤學習任務，降低單頭飽和/退化風險；即使個別頭不佳，其它頭仍能補位（**容錯性**）。
4. **跨尺度/跨距離依賴的捕捉**
   - 不同頭可偏好**局部 vs. 長距**依賴，或在多模態中專注不同模態/區域的對齊。
5. **並行計算，計算成本友善**
   - 在固定 \(d_{\text{model}}\) 下，將維度分給 \(h\) 個頭通常保持相近 FLOPs，**易於並行**。
6. **設計空間大、靈活組合**
   - 可在頭層級搭配**相對/旋轉位置編碼**、**稀疏/區塊注意力**、**局部窗/全局頭**等策略。
7. **一定程度的可解釋性**
   - 注意力圖常呈現可觀察模式（如語法對齊、主謂呼應、翻譯對齊），便於分析與調試。

---

## 2) 公式（回顧）
多頭注意力：

$$
\mathrm{MHA}(Q,K,V)
= \mathrm{Concat}_i\big(\mathrm{Attn}(QW_Q^{(i)},KW_K^{(i)},VW_V^{(i)})\big)\,W_O
$$

單頭縮放點積注意力：

$$
\mathrm{Attn}(Q,K,V)=\mathrm{softmax}\!\left(\frac{QK^\top}{\sqrt{d_k}}+\mathrm{mask}\right)V
$$

---

## 3) 推論效能的實務優化
- **MQA（Multi-Query Attention）**：多頭各自有 \(Q\)，但**共享同一組 \(K,V\)** → **顯著降低記憶體/快取帶寬**、推論更快，品質接近 MHA。
- **GQA（Grouped-Query Attention）**：多個頭分組共享 \(K,V\)，在效能與品質間**折衷**。
- **Head Pruning / Head Dropout**：修剪冗餘頭以**壓縮模型**、**提速**；不少頭功能重疊，修剪影響有限。

---

## 4) 常見誤區
- **頭越多不一定越好**：若資料量、正則化、總維度配置不當，過多的頭可能冗餘或不穩。
- **多頭 ≠ 多步**：多頭是**同一層的並行注意**，不是跨時間步的推理；長程規劃仍仰賴層數與整體結構。
- **計算不一定線性增加**：在固定 \(d_{\text{model}}\) 下，增加頭數更多影響**記憶體訪問模式**與常數因子，效能需實測。

---

## 5) 30 秒速答（面試/口試）
> **多頭注意力把注意力拆成多個子空間並行計算，每個頭學不同的關係模式；最後再拼接與線性混合。這讓模型同時捕捉語法、語義與位置等多種依賴，提升表達力與有效秩；在固定總維度下仍能高效並行。實務上可用 MQA/GQA 降記憶體、提速度。**
